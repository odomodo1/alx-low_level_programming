#include "main.h"

/**
 * _abs - computes the absolut value of integer
 * @f: the integer to be computed
 * Return: the absolute value
 */
int _abs(int f)
{
	if (f >= 0)
		return (f);
	else
		return (-f);
}
